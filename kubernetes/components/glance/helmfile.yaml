repositories:
  - name: rubxkube
    url: https://rubxkube.github.io/charts/

releases:
  - name: glance
    namespace: glance
    chart: rubxkube/glance
    version: 0.0.9
    values:
      - values.yaml
